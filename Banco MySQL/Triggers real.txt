CREATE DATABASE Aprendendo_TRIGGER;

CREATE TABLE produto(
id_produto INT NOT NULL AUTO_INCREMENT PRIMARY KEY,
nome VARCHAR(256),
preco DECIMAL(8.2),
estoque INT
);

CREATE TABLE pedido(
id_pedido INT NOT NULL AUTO_INCREMENT PRIMARY KEY,
data_horario DATETIME,
quantidade INT,
produto_id INT,
FOREIGN KEY (produto_id) REFERENCES produto (id_produto)
);

INSERT INTO produto
(nome,preco,estoque)
VALUES
('caderno','19.00','200'), 
('caneta','5.00','500'),
('lápis','3.00','1000');

-- Aprendendo a utilizar os gatilhos
DELIMITER $$
CREATE TRIGGER estoque_atualizado
AFTER INSERT ON pedido -- depois de inserir na tabela pedido
FOR EACH ROW -- para cada linha
BEGIN -- comece
UPDATE produto -- atualize
SET produto.estoque = produto.estoque - new.quantidade -- setar no estoque o estoque menos a quantidade pedida pelo cliente
WHERE produto.id_produto = new.produto_id; 
END;
$$ 

INSERT INTO pedido
(data_horario,quantidade, produto_id)
VALUES
('2025-10-16 19:30:00', 49, 1);

SELECT * FROM pedido;
SELECT * FROM produto;



